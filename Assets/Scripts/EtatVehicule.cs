using UnityEngine;
using UnityEngine.UI;
using TMPro;

public class EtatVehicule : MonoBehaviour

   {
    //actualize l'état du vehicule, 3 états possible; finalement, non-utilisé car remplacé par la healthBar

    /*public Text etatVehicText;
    public static int etatVehicule;

    // Update is called once per frame
    void Update()
    {
        etatVehicText.text = GameControler.etatVehicule.ToString();
    }*/
    }

