using System;
using Unity.VisualScripting;
using UnityEngine;

public class ScriptBonus : MonoBehaviour
{
    

    /*void Start()
    {
        state = BonusStatus.noBonus;
        bonusType = BonusType.noBonus;
    }*/
    /*void Update()
    {
        if (type != "null")
        {
            timer += Time.deltaTime;
            if (timer > 5f)
            {
                DesactivateBonus();
                Destroy(gameObject);
            }

        }
    }*/
    

    /*public void ActivateBonus()
    {
        //Debug.Log($"Bonus {bonusType} activated");
        if (type == "invulnerable")
        {
            Debug.Log("Invulnerable bonus activated");
        }
        else if (type == "health")
        {
            Debug.Log("Health bonus activated");
        }
        else if (type == "speed")
        {
            Debug.Log("Speed bonus activated");
        }
        else if (type == "magnetic")
        {
            Debug.Log("Magnetic bonus activated");
        }
        type = "null";


    }*/
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created

}
